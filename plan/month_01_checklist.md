# План на первый месяц — пошаговый чек‑лист (дни 1–28)

*Цель месяца:* получить устойчивую базу в Python/Pandas/NumPy, настроить окружение и GitHub, завершить мини‑проект по EDA и начать подготовку к ML‑проекту.

Каждый день: делай пометки в чек‑боксе `- [ ]` → `- [x]` после выполнения. По средам и субботам — короткий чек‑ин (отправить ссылку на репозиторий / ноутбук ментору).

---

## Недели и краткие цели

| Неделя |                                           Цель | Ключевой результат                                            |
| ------ | ---------------------------------------------: | ------------------------------------------------------------- |
| 1      |       Настройка окружения, Git, базовый Python | Рабочее окружение + пустой репозиторий `ml-track` с README    |
| 2      |                Pandas / numpy — практика + EDA | Первый Jupyter Notebook: простая EDA и визуализации           |
| 3      |   Базовая ML-цепочка (scikit-learn) + SQL ввод | Мини‑модель (baseline) для табличного датасета                |
| 4      | Доработка мини‑проекта, документация, LeetCode | Репозиторий с project-notebook + README, 15–20 решённых задач |

---

## День 1 — настроить базу (окружение + GitHub)

* [ ] Установить/обновить Python 3.10+ (или убедиться, что он установлен).
* [ ] Установить conda или создать виртуальное окружение:

  ```bash
  conda create -n ml python=3.10 -y
  conda activate ml
  # или venv
  python -m venv .venv
  source .venv/bin/activate   # linux/mac
  .\.venv\Scripts\activate  # windows
  ```
* [ ] Установить базовые пакеты:

  ```bash
  pip install --upgrade pip
  pip install jupyterlab numpy pandas matplotlib seaborn scikit-learn notebook
  pip install jupyterlab
  ```
* [ ] Создать GitHub аккаунт (если нет) и публичный репозиторий `ml-track`.
* [ ] Инициализировать локально git, сделать первый commit с `README.md`:

  ```bash
  git init
  git remote add origin git@github.com:YOUR_LOGIN/ml-track.git
  echo "# ml-track" > README.md
  git add README.md
  git commit -m "chore: init repo and README"
  git branch -M main
  git push -u origin main
  ```

## День 2 — базовый Python и VSCode

* [ ] Настроить VSCode (Python extension) и Jupyter (или JupyterLab).
* [ ] Пройти 1–2 урока Kaggle: Python (вводные). Делать заметки.
* [ ] Создать папку `notebooks/` и файл `notebooks/01_python_intro.ipynb`.
* [ ] Запушить изменения (pull → commit → push).

## День 3 — начни Pandas

* [ ] Пройти уроки Kaggle: Pandas — первые 2 раздела.
* [ ] В `notebooks/02_pandas_basics.ipynb` выполнить: читать CSV, head(), describe(), missing values.
* [ ] Сделать commit и PR (создать ветку `pandas-basics` → PR в `main`).

## День 4 — численные операции (NumPy)

* [ ] Пройти краткий туториал по NumPy (векторы, матрицы, broadcasting).
* [ ] В notebook: решить 5 упражнений с массивами (reshape, sum, mean, axis).
* [ ] Push + краткий комментарий в PR.

## День 5 — визуализации и EDA навыки

* [ ] Повторить matplotlib и seaborn: гистограммы, scatter, boxplot.
* [ ] Сделать простую EDA‑визуализацию на выбранном CSV (распределение, выбросы).
* [ ] Записать выводы в Markdown ячейке (что интересно, подозрительно, какие признаки важны).

## День 6 — выбрать датасет для мини‑проекта (EDA)

* [ ] Найти 1 CSV (или Kaggle dataset) для табличного мини‑проекта (небольшой — 5k–50k строк).
* [ ] Создать `projects/tabular_eda` с `notebooks/00_dataset_description.md`.
* [ ] Сохранить копию датасета в `data/` (или ссылку в README).

## День 7 — длинная сессия: начать EDA проект

* [ ] Выполнить полную EDA: missing values, типы данных, корреляции, базовые преобразования.
* [ ] Сформулировать 2‑3 гипотезы (что предсказывать и почему).
* [ ] Закоммитить промежуточный результат.

---

## День 8 — повтор и LeetCode

* [ ] Просмотреть результаты первой недели, исправить замечания (если были).
* [ ] Решить 2 easy задачи на LeetCode (лог/масивы/string basics).
* [ ] Пройти следующие 1–2 урока Kaggle по Pandas.

## День 9 — преобработки и фичи

* [ ] На EDA проекте: обработать пропуски (стратегии), закодировать категориальные признаки.
* [ ] Добавить базовые feature engineering: даты → признаки, агрегаты, ratios.

## День 10 — train/test split и baseline

* [ ] Разбить данные (train/test) — применить простую модель: LogisticRegression / RandomForest.
* [ ] Оценить метрики (accuracy, ROC-AUC, confusion matrix). Сделать baseline.
* [ ] Закоммитить notebook с результатами.

## День 11 — валидация и cross‑validation

* [ ] Добавить cross‑validation (KFold/StratifiedKFold) и сравнить результаты.
* [ ] Сравнить несколько простых моделей (logreg, rf, xgboost — если доступен).

## День 12 — интерпретация модели

* [ ] Построить важность признаков (feature importances) и несколько SHAP/или простых графиков объяснения.
* [ ] Выбрать улучшение для следующей итерации.

## День 13 — чистка кода и README

* [ ] Рефакторинг ноутбуков: вынести подготовку данных в `scripts/data_prep.py` (простая версия).
* [ ] Заполнить `projects/tabular_eda/README.md` с кратким описанием и ссылками на ноутбук.

## День 14 — чек‑ин с ментором (среда)

* [ ] Отправить ссылку на репо и notebook ментору для ревью.
* [ ] Получить список правок и сделать исправления.

---

## День 15 — ML улучшения

* [ ] Попробовать гиперпараметризацию (GridSearchCV / RandomizedSearchCV) на выбранной модели.
* [ ] Оценить прирост метрик.

## День 16 — эксперимент с признаками

* [ ] Добавить 2–3 новых признака (агрегаты/статистики) и проверить влияние.

## День 17 — контейнеризация (ввод)

* [ ] Изучить базовый Dockerfile (чтобы понимать концепцию). Создать `Dockerfile` с минимальным приложением (опционально).

## День 18 — SQL мини‑обзор

* [ ] Пройти 1 час практики SQL (SELECT, WHERE, JOIN, GROUP BY).
* [ ] Применить SQL‑подход к CSV (sqlite3 или pandas.read_sql).

## День 19 — тестирование результатов

* [ ] Написать простой sklearn pipeline и убедиться, что предсказание воспроизводимо (seed, версионирование).

## День 20 — доклад и презентация проекта

* [ ] Подготовить короткую презентацию (5–7 слайдов) по mini‑project: цель, данные, методы, результаты, выводы.

## День 21 — длинная сессия: polish

* [ ] Доработать README, добавить GIF/скринкаст результатов (как запустить ноутбук, как посмотреть выводы).
* [ ] Проверить структуру репозитория: `data/`, `notebooks/`, `scripts/`, `README.md`, `LICENSE`.

---

## День 22 — LeetCode + рефлексия

* [ ] Решить 3 easy/1 medium задачи на LeetCode.
* [ ] Перечитать заметки по Pandas/NumPy, воспроизвести 2 ключевых приёма (в ноутбуке).

## День 23 — подготовка к scikit‑learn pipeline

* [ ] Ознакомиться с `ColumnTransformer` и `Pipeline`.
* [ ] Переписать модель в виде pipeline (preprocessing + estimator).

## День 24 — сравнение моделей

* [ ] Попробовать LightGBM/XGBoost (установить пакет, обучить) и сравнить с baseline.

## День 25 — документация и clean commits

* [ ] Пройтися по истории коммитов, сделать аккуратные сообщения (squash если нужно), обновить CHANGELOG в README.

## День 26 — подготовка к DL (введение)

* [ ] Установить PyTorch (или TF) локально и выполнить минимальный пример (тензор + GPU проверка).
* [ ] Пройти вводный урок по transfer learning (fast.ai или tutorial PyTorch).

## День 27 — публикация и ссылки

* [ ] Подготовить краткий пост/текст для LinkedIn (скелет) о завершённом мини‑проекте.
* [ ] Убедиться, что репозиторий публичный и все результаты воспроизводимы.

## День 28 — итог недели + чек‑ин (суббота)

* [ ] Отправить финальную ссылку на репозиторий ментору для проверки.
* [ ] Составить план на второй месяц (что улучшить, какие модули изучить дальше).

---

# Контроль качества: что должно быть в репозитории к концу месяца

* [ ] `README.md` с кратким описанием проекта и инструкциями по запуску.
* [ ] `notebooks/` с подписанными шагами EDA и моделью.
* [ ] `data/` с описанием источника (или `data/README.md` с ссылкой).
* [ ] `scripts/` с базовыми ETL/prepare скриптами (если есть).
* [ ] корректные `.gitignore` и аккуратные коммиты.

---

# Советы по работе с чек‑листом

1. Коммить каждый день — даже небольшие шаги. Коммиты — это доказательство прогресса.
2. Пиши понятные сообщения и короткие описания в README: кто, что, как запускать.
3. Если застрял — сделай короткую запись (снимок экрана / заметку) и отправь ментору на ревью.
4. Не стремись к идеалу: цель — работоспособный и объяснимый результат.

---

Если хочешь, я также могу:

* создать шаблон `README.md` и `notebooks/` (готовые заголовки),
* или сгенерировать `Dockerfile` / `requirements.txt` для проекта.

Скажи, что сделать дальше: `README` шаблон, `Dockerfile`, или хочу чтобы ты прислал ссылку на GitHub и я сразу сделал ревью.
